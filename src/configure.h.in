#ifndef APP_CONFIGURE_H_IN_H_
#define APP_CONFIGURE_H_IN_H_

#cmakedefine DISABLE_AUTORUN
#cmakedefine AUTORUN_IMMEDIATELY
#cmakedefine ENABLE_SHUTDOWN
#cmakedefine SKIP_TESTS_BY_DEFAULT
#cmakedefine RUNTIME_CONFIG_PATH "@RUNTIME_CONFIG_PATH@"
#cmakedefine DEFAULT_OUTPUT_DIRECTORY_PATH "@DEFAULT_OUTPUT_DIRECTORY_PATH@"


#ifdef DISABLE_AUTORUN
#define DEFAULT_DISABLE_AUTORUN true
#else
#define DEFAULT_DISABLE_AUTORUN false
#endif

#ifdef AUTORUN_IMMEDIATELY
#define DEFAULT_AUTORUN_IMMEDIATELY true
#else
#define DEFAULT_AUTORUN_IMMEDIATELY false
#endif

#ifdef ENABLE_SHUTDOWN
#define DEFAULT_ENABLE_SHUTDOWN true
#else
#define DEFAULT_ENABLE_SHUTDOWN false
#endif

#ifdef SKIP_TESTS_BY_DEFAULT
#define DEFAULT_SKIP_TESTS_BY_DEFAULT true
#else
#define DEFAULT_SKIP_TESTS_BY_DEFAULT false
#endif

#endif  // APP_CONFIGURE_H_IN_H_
